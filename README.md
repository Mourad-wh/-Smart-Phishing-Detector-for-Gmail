# -Smart-Phishing-Detector-for-Gmail
An automated web application that analyzes Gmail emails in real time using a machine learning model (TF-IDF + supervised classification). The system calculates a risk score for each email, identifies suspicious URLs, and reduces false positives.
